<Window x:Class="PCDoctor.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:PCDoctor.UI"
        mc:Ignorable="d"
        Title="SettingsWindow" Height="450" Width="800">
    <Grid Margin="24">
        <StackPanel>
            <TextBlock Text="Settings"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="0,0,0,24"/>

            <TextBlock Text="API Base URL" Foreground="#A9B7D8"/>
            <TextBox x:Name="ApiBaseUrlTextBox" Margin="0,6,0,16"/>

            <TextBlock Text="Refresh Interval (seconds)" Foreground="#A9B7D8"/>
            <TextBox x:Name="RefreshIntervalTextBox" Margin="0,6,0,16"/>

            <TextBlock Text="API Upload Interval (seconds)" Foreground="#A9B7D8"/>
            <TextBox x:Name="ApiSendIntervalTextBox" Margin="0,6,0,16"/>

            <Button Content="Save"
                    Width="120"
                    Height="36"
                    HorizontalAlignment="Right"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
