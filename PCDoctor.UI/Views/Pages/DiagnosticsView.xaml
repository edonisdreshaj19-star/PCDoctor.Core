<UserControl x:Class="PCDoctor.UI.Views.Pages.DiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#0B1020"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="230"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="370"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                            VerticalAlignment="Center">
                    <TextBlock Text="Diagnostic Report"
                               FontSize="26"
                               FontWeight="Bold"/>

                    <TextBlock Text="Generate a full system diagnosis based on the latest collected stats."
                               Foreground="#8F9BB8"
                               FontSize="14"
                               Margin="0,8,0,0"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="View Reports"
                        Command="{Binding OpenReportsCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        MinWidth="145"
                        VerticalAlignment="Center"/>

                <Button Grid.Column="3"
                        Content="{Binding Diagnostics.DiagnosticReportButtonText}"
                        Command="{Binding GenerateDiagnosticReportCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        MinWidth="145"
                        VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Grid.Column="0"
                Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Health Score"
                               Style="{StaticResource SectionTitleStyle}"
                               VerticalAlignment="Center"/>

                    <Border Grid.Column="1"
                            Background="#222B45"
                            CornerRadius="12"
                            Padding="14,8"
                            MinWidth="96"
                            MinHeight="34"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                        <TextBlock Text="{Binding Diagnostics.DiagnosticReportStatusText}"
                                   Foreground="{Binding Diagnostics.DiagnosticReportStatusBrush}"
                                   FontSize="13"
                                   FontWeight="Bold"
                                   TextAlignment="Center"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"/>
                    </Border>
                </Grid>

                <TextBlock Grid.Row="1"
                           Text="{Binding Diagnostics.DiagnosticReportScoreText}"
                           FontSize="46"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="0,8,0,8"/>

                <ProgressBar Grid.Row="2"
                             Value="{Binding Diagnostics.DiagnosticReportProgressValue}"
                             Foreground="{Binding Diagnostics.DiagnosticReportStatusBrush}"
                             Margin="0,0,0,4"/>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Grid.Column="1"
                Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Summary"
                           Style="{StaticResource SectionTitleStyle}"/>

                <TextBlock Grid.Row="1"
                           Text="{Binding Diagnostics.DiagnosticReportCreatedAtText}"
                           Foreground="#6F7FA6"
                           FontSize="12"
                           Margin="0,14,0,14"/>

                <TextBlock Grid.Row="2"
                           Text="{Binding Diagnostics.DiagnosticReportSummaryText}"
                           Foreground="#D7DEF5"
                           FontSize="15"
                           TextWrapping="Wrap"
                           LineHeight="22"/>
            </Grid>
        </Border>

        <Border Grid.Row="4"
                Grid.Column="0"
                Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Detected Issues"
                           Style="{StaticResource SectionTitleStyle}"/>

                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Margin="0,18,0,0">
                    <ItemsControl ItemsSource="{Binding Diagnostics.ReportDetectedIssueItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           Foreground="#D7DEF5"
                                           FontSize="14"
                                           TextWrapping="Wrap"
                                           LineHeight="22"
                                           Margin="0,0,8,12"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <Border Grid.Row="4"
                Grid.Column="1"
                Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Recommendations"
                           Style="{StaticResource SectionTitleStyle}"/>

                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Margin="0,18,0,0">
                    <ItemsControl ItemsSource="{Binding Diagnostics.ReportRecommendationItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           Foreground="#D7DEF5"
                                           FontSize="14"
                                           TextWrapping="Wrap"
                                           LineHeight="22"
                                           Margin="0,0,8,12"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>